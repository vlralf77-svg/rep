# 스포츠 예측 앱 개발 작업 내용

## 프로젝트 개요

스포츠토토(배트맨토토) 스타일의 경기 예측 웹앱 + Android APK를 개발한 작업 내용 정리.

---

## 기술 스택

| 항목 | 내용 |
|------|------|
| Frontend | React 18 + Vite + TypeScript + MUI v5 (다크 테마) |
| 상태관리 | TanStack Query + Zustand |
| Backend | Node.js + Express + Prisma + SQLite (최종적으로 APK에서 미사용) |
| APK | Capacitor v5 (appId: `com.sportstoto.predict`) |
| 모노레포 | pnpm workspaces (apps/web, apps/api, packages/shared) |
| CI/CD | GitHub Actions |

---

## 구현된 기능

### 1. 대시보드 (DashboardPage)
- 축구 / KBO 야구 탭 전환
- 리그 필터 (축구: PL, BL1, PD, FL1, SA 5개 리그)
- 경기 상태 필터 (전체 / 예정 / 종료)
- 날짜별 그룹핑
- 동기화 버튼

### 2. 경기 상세 (MatchDetailPage)
- 홈/원정 팀 정보
- 예측 확률 차트 (승/무/패)
- 예상 스코어 TOP 5
- 예상 득점

### 3. 배팅 마켓 예측 (BettingMarketsCard)
배트맨토토 스타일로 다음 항목 예측:
- 풀타임 승/무/패
- 풀타임 오버/언더 (1.5 / 2.5 / 3.5)
- 전반전 승/무/패
- 전반전 오버/언더 (0.5 / 1.5)
- KBO 야구: 승/패 (무승부 없음)

### 4. 예측 엔진 (predict.ts)
- 포아송 분포 기반 득점 확률 계산
- Elo 레이팅 블렌딩 (포아송 70% + Elo 30%)
- 전반/후반 득점 분포 추정

### 5. 데이터 소스
- **축구**: football-data.org API (API 키: `6942278d1b1e447bb04375f4b84ce286`)
- **KBO 야구**: TheSportsDB API (무료 티어, 리그 ID: 4342)
- 데이터 로컬 캐시: `localStorage` (`sports_data_v1`)

---

## APK 개발 과정

### 초기 구조 (백엔드 의존)
```
APK → localhost:3001 (Express API) → football-data.org / TheSportsDB
```
**문제**: APK가 폰에 설치되면 `localhost:3001`에 접근 불가 → 데이터 없음

### 최종 구조 (Standalone)
```
APK → football-data.org (직접 호출)
APK → TheSportsDB (직접 호출)
예측 → 클라이언트 사이드 계산 (predict.ts)
```
- Capacitor Native HTTP 플러그인으로 CORS 우회

---

## 해결한 오류들

| 오류 | 원인 | 해결 |
|------|------|------|
| pnpm-lock.yaml 없음 | lockfile 미생성 | `pnpm install` 후 커밋 |
| lockfile 형식 불일치 | CI pnpm v8 vs 로컬 v10 | CI를 pnpm v10으로 업그레이드 |
| @sports/shared not found | `dist/index.js` 없음 | `main`을 `./src/index.ts`로 변경 |
| TypeScript 빌드 오류 | `noUnusedLocals` 등 strict 옵션 | tsconfig.json에서 제거 |
| APK 설치 불가 ("앱을 설치할 수 없음") | 알 수 없는 출처 차단 | 폰 설정에서 출처 불명 앱 허용 필요 |
| APK에서 데이터 없음 | localhost:3001 접근 불가 | Standalone 구조로 전환 |
| BettingMarkets isBaseball 오류 | props 구조분해 누락 | 함수 시그니처에 추가 |
| 빌드 오류 (favicon) | index.html에 없는 파일 참조 | favicon 링크 제거 |

---

## 결론: 데이터가 안 나오는 이유

### KBO 야구 데이터 없음
**근본 원인**: TheSportsDB **무료 티어**는 KBO 실시간/최신 경기 데이터 제공이 매우 제한적

- `eventsseason`, `eventsnextleague`, `eventspastleague` 등 3개 엔드포인트 호출
- 응답은 오지만 KBO 경기 데이터 자체가 없거나 비어있음
- TheSportsDB 유료 Patreon ($5/월 이상) 가입 시 더 많은 데이터 접근 가능

### 축구 데이터
- football-data.org API는 정상 작동
- 단, APK(Android 앱)에서 CORS 문제가 발생할 수 있음
- Capacitor Native HTTP가 활성화되어 있으나, 빌드 후 실제 폰 테스트 필요

### APK 재빌드 필요
Standalone 구조로 전환 후 GitHub Actions에서 새 APK 빌드가 필요함

---

## 파일 구조

```
rep/
├── apps/
│   ├── web/                          # React 프론트엔드
│   │   ├── src/
│   │   │   ├── lib/
│   │   │   │   ├── api.ts            # 직접 API 호출 + localStorage 캐시
│   │   │   │   └── predict.ts        # 클라이언트 예측 엔진
│   │   │   ├── api/
│   │   │   │   └── hooks.ts          # TanStack Query 훅
│   │   │   ├── components/
│   │   │   │   ├── BettingMarkets.tsx
│   │   │   │   ├── MatchCard.tsx
│   │   │   │   └── PredictionChart.tsx
│   │   │   └── pages/
│   │   │       ├── DashboardPage.tsx
│   │   │       └── MatchDetailPage.tsx
│   │   └── capacitor.config.ts       # Capacitor 설정
│   └── api/                          # Express 백엔드 (APK에서 미사용)
├── packages/shared/                  # 공유 타입
└── .github/workflows/
    ├── ci.yml                        # 웹 빌드 CI
    └── apk.yml                       # Android APK 빌드
```

---

## 향후 개선 방향

1. **KBO 데이터**: TheSportsDB Patreon 가입 또는 KBO 공식 API / 스크래핑 대안 검토
2. **축구 데이터**: APK에서 실제 CORS 우회 테스트 확인
3. **캐시 전략**: 동기화 후 만료 시간 설정 (현재는 수동 동기화만)
4. **오프라인 지원**: 마지막 동기화 데이터로 오프라인 사용 가능하게 개선
